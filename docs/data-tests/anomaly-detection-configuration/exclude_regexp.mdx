---
title: "exclude_regexp"
sidebarTitle: "exclude_regexp"
---

`exclude_regexp: [regex]`

Param for the `all_columns_anomalies` test only, which enables to exclude a column from the tests based on regular expression match.

- _Default: None_
- _Relevant tests: `all_column_anomalies`_
- _Configuration level: test_

<RequestExample>

```yml test
models:
  - name: this_is_a_model
    tests:
      - elementary.column_anomalies:
          exclude_regexp: ".*SDC$"
```

</RequestExample>
