4. Now we need to configure the relevant permissions for this new service account. There are a couple of options

- Option 1 - Choose BigQuery Admin or Owner - Not recommended, but it will work.
- Option 2 - Choose the following roles: BigQuery Job User, BigQuery User, BigQuery Data Viewer.

5. The last step is optional, skip it and press done.
