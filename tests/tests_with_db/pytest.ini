[pytest]
markers =
    skip_targets(targets): skip test for the given targets
    only_on_targets(targets): skip test for non given targets
    requires_dbt_version(version): skip test if dbt version is not satisfied
